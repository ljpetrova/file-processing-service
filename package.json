{
  "name": "file-processing-service",
  "description": "",
  "version": "0.1.0",
  "dependencies": {},
  "devDependencies": {
    "serverless": "^2.72.3",
    "serverless-better-credentials": "^1.1.1",
    "serverless-offline": "^8.5.0",
    "serverless-plugin-aws-alerts": "^1.7.1",
    "serverless-python-requirements": "^6.0.0"
  },
  "scripts": {
    "start": "AWS_SDK_LOAD_CONFIG=true AWS_PROFILE=develop AWS_XRAY_SDK_ENABLED=false npx sls offline --stage test --functionCleanupIdleTimeSeconds 5 --useChildProcesses"
  }
}
